# GUI Workstreams, Isolation & Surfacing

- TopBar: new projectName prop renders 'Odo · <project> · <workstream>' with 200px truncation, resolved via projects.find(p => p.root === activeProjectRoot)?.name falling back to daemon project.name — closes the ambiguity that caused the false cross-project /panel alarm (main-epoch-8) (main-epoch-7)
- Sidebar stuck/leaking create-input root cause: `creating` was a single boolean so the box followed whichever project became active; fixed with onBlur→resetCreate (click-away dismiss) + useEffect on activeProjectRoot; accepted tradeoff: first click outside only dismisses (layout shift eats the click), second click switches (main-epoch-8)
- Phantom-project lifecycle: a mktemp git repo was auto-registered by ensureProjectRegistered and 5s pending_counts polling on its dead socket kept respawning its daemon; the registry guard isLinkedGitWorktree now refuses auto-registration of .git files pointing at /.git/worktrees/ (submodule .git/modules/ still allowed); existing rows must be removed manually once via sidebar remove-project (M11 F8) (main-epoch-6)
- Running-badge cross-talk was pure frontend: state keyed by workstream id (ids restart at 1 per journal) was not cleared on project switch → false 'running' for up to ~6s; fixed by resetProjectAggregates clearing 6 project-level aggregate states on root switch + mid-flight root check in refreshPendingCounts (main-epoch-6)
- Workstream-switch scroll repin: applyBootstrap wholesale-replaces events but ChatSurface does not remount, so stickRef=false leaked across sessions; fixed by a conversationId-change effect resetting stick + clearing the pill + pinToBottom; the regression test needs unequal-length histories (12 vs 72) because equal lengths coincidentally land at the bottom and mask the bug (bug-fix-epoch-2)
- Background-run visibility is derived from pending_counts + running_workstreams only (no daemon task registry yet): bgNotice watches the raw runningWorkstreams set (first observation seeds baseline, 4s TTL), Sidebar sorts Needs-input → Working → Idle with stable ties, and bg rows honestly show only 'still running' because non-focused conversations are never polled — a 'Done' tier is deliberately unimplemented until the daemon wave lands (gui-wave-epoch-1)
- StatusBar/stats wave: context-pressure meter reverse-scans existing GUI events for the latest total_prompt_bytes (journaled only on user_message + review_action{run_prompt}) — zero new IPC; agent_done carries only {summary} (OMP usage dropped at the adapter seam) so the turn-stats strip honestly shows wall time + in/out bytes and renders tok/s only if the daemon ever journals usage; LedgerPanel renders review_action rows as receipt cells with a class→level severity map and risk-unrated dashed-neutral that never masquerades as clean (gui-wave-epoch-2) (gui-wave-epoch-1)
- Composer polish (diff #18): container flex-col gap-1.5, removed SteerQueue mx-4 misalignment, shadow-soft token on input form + both queue cards, self-margins stripped from docked chips — browser-verified uniform geometry (all boxes x=256/w=1008, 6px gaps) (main-epoch-6)
