# Control-Plane Hardening & Loop Subsystem

- D1 gate policy: two-tier structural gate boundary replacing the hand-written 10-file protectedGateFiles map — Tier-0 human-only (gatepolicy.go + gate_manifest.json, editing IS the exemption grant) and Tier-1 path prefixes; a startup drift latch recomputes Tier-0 sha16 and fails autoLand/settle closed with reason gate_policy_drift on mismatch until a human runs `odo gate re-pin` (bug-fix-epoch-14)
- C0 purity guard: autonomy.go classifyDiff stays memory-prefix-only — gate-source hits are NOT folded into the autonomy ladder (would pollute it); pinned by a dedicated test (bug-fix-epoch-14)
- Loop fix pipeline (Mode A): classification order is memory prefix → Tier-0 (suspend loop_suspended{cause:risk:gate_core}) → supply-chain → Tier-1 routed through s.autoLand verbatim (inherit-never-fork); the verify-only landing path for loop fixes was deleted (bug-fix-epoch-14)
- Fold attribution is fail-closed: accept/blocked rows attribute to an open Mode A fix phase iff actor ∈ {loopActor, autoActor} AND a loop_diff_bound row names the diff; no binding ⇒ no attribution (bug-fix-epoch-14)
- D3 token ledger: loop budget constrains REAL measured executor cost summed from per-assistant-message usage in session JSONL files; budgeted spend = input + output + cache_write (cacheRead journaled but never budgeted); a spawn row's prompt_tokens_est stays pending until its covering loop_run_usage row lands, then the estimate is replaced; missing transcript ⇒ usage_available:false fail-soft with the estimate left pending (bug-fix-epoch-15)
- Budget enforcement placement: after a usage row lands, spent > budget journals loop_budget_exceeded and suspends BEFORE autoLand; duplicate usage rows are newest-per-spawnSeq idempotent; GUI mirrors the identical additive fold rule in loop.ts (bug-fix-epoch-15)
- D5 finding identity: fingerprint v4 = sha16(norm(file)|norm(symbol)|category[|rule]) — structural identity, not wording, so model-wording drift can no longer create phantom findings; title/evidence are demoted to mutable description (most-severe sighting wins, never hashed) and category stays identity (never severity) (bug-fix-epoch-16)
- D5 dedup order: each leg's findings are FP-deduped first (max severity kept), then folded across legs; Legs counts distinct legs; additive leg_ids aligns to journaled legs[] fan-out positions for falsifiability; migration is append-only — old v3 fingerprints stay as historical identifiers and findings crossing the boundary simply count as new for one round (bug-fix-epoch-16)
- Loop admission atomicity: state fold + loop_started journal append happen in one critical section (early fold retained only as fast-reject hint); proven with a 4-goroutine single-winner race test (main-epoch-15)
- Boot-stranded-intent Sweep path repairs at sweep time: sweepPendingBatch's consumed early-return takes memMu, re-folds the journal fresh, and replays the receipt so a live failed-write marker heals immediately instead of waiting for the next apply or restart (bug-fix-epoch-8)
- Conflict retirement fires only under project-wide fold context (evalScope gate inside retireSupersededConflicts, single point); legacy terminal-landed receipts also retire older open conflicts on the layer; archived lanes remain actionable because delete is a status flip with no status predicate in the resolution chain (bug-fix-epoch-8)
- CommitFold runs epoch bump + marker in one transaction; ListEvents narrowing uses LatestFoldBoundary (no-pin markers fall back to firstSeq=0 full history so the sweep's pre-pin rescue purpose isn't broken); loop fold does one decode per row instead of 2–4 per-key decodes (main-epoch-23)
- Token estimation: 87K-panel breaker underestimated CJK ~3× (len/4); estimatePromptTokens now counts ASCII/4 + 1 token per non-ASCII rune, with an 85K-old-pass/110K-new-block e2e pin; escalation records re-paid input tokens of abandoned requests per bump (main-epoch-23)
