# Verify Gate Diagnosis & Test Flake Triage

- capDetail switched from head-trim to tail-bias (4KB cap, rune-safe cut, …[earlier truncated] prefix) so `--- FAIL` lines survive in blocked journal rows; previously the failing test name was unrecoverable without same-bytes reproduction (main-epoch-26)
- Full gate output is persisted to .odo/verify/<label>-<ts>.log (1MB tail cap per file, pruned to newest 32) with the path appended to journal detail after capping so it can never be truncated away; the success path is also persisted via the verify_log moa field (main-epoch-36)
- Block-triage doctrine: verify archive sha16 identity across journal rows and disk recompute, attribute payload bytes path-by-path to the diff, then run a same-bytes reproduction before classifying a block as a false positive (main-epoch-35) (main-epoch-36) (main-epoch-37)
- Load-flake class: gate verify running concurrently with idle distill (model calls + wiki commits) produces log-flood test failures in internal/ipc; a same-bytes rerun going green means the rerun has zero information gain (main-epoch-36)
- Environment-sensitive test defects fixed: scratch-HOME verify needs explicit review: arming in prefs (main-epoch-25); TestPlaywrightBrowsersDir must scrub PLAYWRIGHT_BROWSERS_PATH via t.Setenv (main-epoch-26); go env subprocesses pinned to GOTELEMETRYDIR under $TMPDIR to kill the async-write cleanup race (main-epoch-26)
- rAF-race e2e class: single-shot assertions race post-commit effects — fixes are expect.poll for "eventually stably visible" contracts (context-panel strip-scroll) and pinning a deterministic window with delayMs before arming failures (switch-cache) (main-epoch-36) (main-epoch-37)
- switch-cache root cause was design-level in gui/src/dev/mock-invoke.ts: the fail branch was evaluated before delayMs, so arming fail rejected bootstrap immediately with no observable window; reordering delayMs-first gives failures a deterministic window while preserving landing-signal semantics (main-epoch-37)
- Poll-dependent e2e assertions use a file-level POLL = {timeout: 12_000} convention matching five sibling specs; RPC-local assertions (textarea restore, error banner) stay on the 5s default because they bypass the 1.5s event poll loop (main-epoch-29) (main-epoch-30)
- Same-class audit rule ("fix one, audit all") applied after every flake fix: swept all e2e specs for arm-fail → immediate single-shot sampling; advisory-slash (web-first) and sidebar (window-pinned absence) were excluded as different classes (main-epoch-37)
- Gate-reported single-spec failures that pass in isolation and on spec rerun are classified as timing flakes and not acted on (pipeline-chip:84 precedent) (main-epoch-22)
