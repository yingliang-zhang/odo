# Outstanding Work Items

- M7 GUI webview E2E (cua-driver) remains outstanding per the log across all epochs (epoch-4)
- `steering.txt` write path is dead code in omp.go; the Adapter interface was not cleaned (noted in A2 brief RC8, superseded by queue-continuation) (epoch-2)
- Earlier unexplained anomalies from midday: agent_error exit 127 (`omp: command not found`), one agent_text output as raw session JSON, and a 401 auth failure — not investigated (epoch-4)
