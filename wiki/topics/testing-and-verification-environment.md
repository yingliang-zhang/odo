# Testing & Verification Environment

- Zombie vite dev servers on port 1420 owned by other worktrees combined with playwright `reuseExistingServer:true` silently e2e-tested the wrong build — producing one false-pass on the scroll fix and one stale-code e2e run; check port ownership before e2e; flipping `reuseExistingServer` to false was recommended but left to the user because it touches cross-worktree parallel workflow habits (bug-fix-epoch-2) (UI-epoch-2)
- Main's red tsc (broken since diff #22) came from `Promise.withResolvers` in `mock-invoke.ts` against an ES2020 lib; fixed in `63d698a` by raising lib to ES2024 and unifying adjacent executor code to `withResolvers` — runtime floor (dev-only file): Chrome 119+/Safari 17.4+/WebKitGTK ≥2.44 (UI-epoch-11)
- The bash tool's `cwd` parameter intermittently failed to take effect: one run produced a false "22 files failed" vitest reading (scanning a worktree with no `node_modules`), another resolved the Playwright config from the wrong directory; stable mitigations are explicit `cd`, absolute paths, or `eval` + `Bun.spawn({cwd})`; reported via `xd://report_issue` (UI-epoch-11) (UI-epoch-2)
- Two stale red tests (`TestGetSettings` after the `auto_apply` off→main default flip, `TestPhantomDiffVerdictBlocksAutoLand` missing a prefs `review:` line) were deliberately not folded into the dedup diff to avoid double-carry rebase conflicts — they rode #22's snapshot instead; pre-existing gofmt debt in `loop_audit`/`loop_journal` was left untouched across epochs (UI-epoch-9) (UI-epoch-10)
- Pipeline stuck-diagnosis anchors: the legal silent window ceiling is ≈25 min (10 min verify timeout + 900 s per panel leg); truly stuck = no moa/auto-land lines in daemon.log + no `review_action` in journal + longer than that; with stage rows landed, a bare "queued" lasting more than a few seconds is genuinely anomalous (bug-fix-epoch-1)
- Mock identity bug: `mock-invoke` passed the fixture array by reference as `running_workstreams`, so in-place e2e mutation made it `Object.is`-equal to React state → bailout with permanently stale UI — the mock now copies arrays at the response edge (gui-wave-epoch-1)
