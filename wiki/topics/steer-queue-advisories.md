# Steer Queue, Advisory Slashes & Composer

- The daemon already was a queue (handleSteering appends to meta.queuedSteers, drained at turn boundary into continuation runs); the GUI work was display-only — floating Processing card, queue list above composer, Drop with two-stage confirm — plus three fail-closed daemon ledger fixes with zero scheduling behavior change (UI-epoch-4)
- Steer ledger: queuedSteers carry journal seqs, run_prompt receipts gain steer_seqs (steer-less lines byte-identical), and steer_dropped{cause} is journaled on every abandon path (cancelled/errored/run_terminal/admission) so every journaled steer ends consumed or dropped; orphan steers with no active run are rejected and never journaled (UI-epoch-4) (UI-epoch-5)
- Drop two-stage confirm must preserve its armed state across derived recomputation from poll heartbeats (UI-epoch-5)
- Steering path proven end-to-end in production: enqueued during active run → agent_done triggers drainRun → run_prompt{origin:continuation, steer_seqs} → continuation run executes, ledger exactly closed (UI-epoch-6)
- Advisory slash submits (/panel, /vision, /preview) are detached from the composer await: the box clears and unlocks immediately, the background promise drives the spinner, and a guarded restore only puts the draft back if the daemon rejects instantly AND the composer is still untouched — accepted semantic change: normal sends are now allowed during a panel consult (bug-fix-epoch-3) (bug-fix-epoch-4)
- /panel hangs were a synchronous daemon RPC (handlePanelQuery wg.Wait for minutes) held across ChatSurface sending=true, which lives on the single global instance and locked every workstream's composer (bug-fix-epoch-3)
- panelThinking is a counter (n±1, render >0) so concurrent advisories share the spinner; panel progress heartbeat (N/M back) is memory-only and never journaled, with poll_events handshakes copying the map to avoid an encode race (bug-fix-epoch-4)
- Advisory submits preserve one-shot park arm: daemon routes slash commands before steer/park branches and the GUI clears with keepPark:true (bug-fix-epoch-4)
- IME blue-box root cause: on WKWebView the Enter confirming a candidate can arrive without isComposing/keyCode 229, submitting mid-composition so compositionend never fires and composingRef stays stuck; fix synchronously clears ta.value and composingRef=false on every successful send (verified at DOM-contract level, not reproducible in Chromium) (UI-epoch-4)
