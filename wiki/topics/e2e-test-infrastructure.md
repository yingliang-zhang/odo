# E2E & Test Infrastructure

- Poll-dependent assertions get `POLL = {timeout: 12_000}`, matching the pre-existing REFRESH convention across five sibling specs; RPC-local assertions keep the 5s default — tagging RPC-local ones would mask mechanism, and introducing a second convention was refused (main-epoch-29) (main-epoch-30)
- Keep-alive changed page-global text matching: hidden mounted panels let `getByText("Diff #1")` match resolved record cards; accept/reject assertions were scoped to the `.review-inbox` container with a documented scoping rule (main-epoch-31)
- Latent environment-dependent test on main: `TestAutoLandStartedRowsAbsentBeforeSpend` lacked scratch-HOME isolation + review arming (the single slip among 24 pipeline cases), so it failed only under verify's scratch HOME once the M20 arming gate existed (main-epoch-25)
- `TestPlaywrightBrowsersDir` was a deterministic env-sensitive defect — verify exports `PLAYWRIGHT_BROWSERS_PATH` when the host has an ms-playwright cache and the test never scrubbed it; it now starts with `t.Setenv("PLAYWRIGHT_BROWSERS_PATH", "")`, proven by an A/B reproduction (main-epoch-26) (main-epoch-27)
- switch-cache:64 was a mock timing race misattributed as an app bug: arming fail raced the mock's fixed 50ms invoke delay; fixtures gained a per-workstream `bootstrapLandings` counter proving toggles were consulted, and the spec waits for the landing signal before arming failure (main-epoch-28)
- History-injection tests need unequal-length histories across sessions (12 vs 72 messages) — equal lengths let browser scroll anchoring coincidentally land at the bottom and mask the repin bug with a false pass (bug-fix-epoch-2)
- Zombie vite dev servers on :1420 from other worktrees plus `reuseExistingServer: true` silently run stale builds producing false passes — check port ownership before running e2e (UI-epoch-2) (bug-fix-epoch-2) (UI-epoch-5)
- Fixture chain coupling: the cap 3→2 change required synchronized ledger.spec.ts updates (row count 11→10, revise-round numbering, ordering index, badge counts) — a repo-wide coupling audit is part of any such constant change (main-epoch-22)
- The bash tool's `cwd` parameter is intermittently ignored, causing wrong-tree runs and false readings (e.g. vitest scanning a worktree with no node_modules); use explicit `cd` or Bun.spawn({cwd}) (main-epoch-26) (UI-epoch-11) (UI-epoch-2)
- Mixed go+gui diffs now run ALL applicable `.odo-verify` matches (was go-only, hiding gui failures), and worktrees auto-provision `gui/node_modules` via symlink from the main checkout — the environmental false positive that blocked diff #8 (UI-epoch-5) (UI-epoch-4)
