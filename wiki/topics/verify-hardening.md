# Verify Observability & Hardening

- capDetail is tail-biased: the 4KB journal cap keeps the tail so '--- FAIL' lines survive, prefixed with a rune-safe '…[earlier truncated]' — head-trimming previously ate the FAIL line and forced blind same-bytes reproductions on diffs #47/#48 (main-epoch-26) (main-epoch-25)
- Full verify output persists to .odo/verify/<label>-<ts>.log (1MB tail cap per file, pruned to newest 32); the journal's capped tail gets the '[full verify output: …]' path appended AFTER capping so it can never be truncated away, and success rows gain a verify_log field on the moa row (main-epoch-36)
- recover-pending-diffs dedups against terminal journal rows: auto_land_blocked (reason not panel_infra), auto_panel moa_review, and auto_revise_round are skipped; auto_land_started/refresh_attempted crumbs and panel_infra re-fire by design (infra's only retry channel); journal read failure abandons the whole pass fail-closed (UI-epoch-9)
- SIGQUIT is discarded via signal.Notify (each delivery logged; SIGABRT retained for deliberate dumps) after an unattributable SIGQUIT killed a daemon mid-panel; graceful-TERM-on-QUIT was rejected because Wait would hang shutdown up to ~24 min on in-flight panels (bug-fix-epoch-4)
- Crash-resilience seams: failpoint hooks on the Server struct enable crash-injection tests of the memory recovery protocol (main-epoch-39)
- Stuck definition: 900s is the per-leg floor and TimeoutForModel (1446s) the per-leg outer ceiling, so true stuck is ~35min/phase — the older ~61min figure was pre-deadline escalation-stack residue and is superseded (main-epoch-23)
- Store migrations: v4 dedupes fossil duplicate workstream names (winner keeps the name, loser gets -dup-<id>) plus a partial unique index on status='active'; the v4 index is excluded from unconditional v1 DDL because legacy DBs need dedupe first (main-epoch-38)
- Parked defects without fixes: old-binary CLI worktree-path bootstrap hang; mock status-flip parity gap in the active poll; unconfirmed App.tsx warm-effect cache pollution (needs a dedicated repro); TERM-exit 'remove socket: no such file' dup; unidentified SIGQUIT sender (main-epoch-27) (main-epoch-31) (main-epoch-28) (bug-fix-epoch-4)
