# D9 Learning Control Plane

- W3 observability: each distill journals one learning_episode review_action row per lane over a marker-pinned [first_seq, last_seq] window with outcome classes; candidates persist to an append-only .odo/learning/candidates.jsonl with a sha256 artifact hash over version/scope/base_sha16/base_source_seq/delta/content; odo learning status renders one daemon-side fold (bug-fix-epoch-30)
- Candidates arise only from accepted learner-batch proposals diverted away from memory.md writes when the learning_stages pref is ON; stage transitions are journaled only (candidate -> shadow on lint+replay pass, shadow -> canary on >=3 main epochs plus re-pass on the grown slice plus a free slot, any -> frozen on R2), and stage status is a journal fold never stored on the row (bug-fix-epoch-32)
- All learning gates are LLM-free and fail-closed: lint (bounds, format, dup-free, retract targets exist, evidence note exists, R2 freeze) and frozen replay with deterministic criteria — missing inputs yield unverifiable = FAIL; the canary seam injects via deterministic ordinal % M == 0 interleave with zero RNG, journaled before the run (bug-fix-epoch-32)
- Never-score-own-changes: one shared excludedFromScoring predicate (gate-source, memory-path, and learning-plane paths) is mirrored into rules-audit cohort/baseline resolution; promotion cohorts exclude auto rows, other canaries, and scoring-excluded rows with paired >=10 human outcomes per leg; the evidence -> measure -> gate separation is AST-pinned so no single evidence row can move a stage (bug-fix-epoch-32)
- W6 actuation: promote --global is human-initiated with zero file I/O — user.md is never written, the CLI returns the rule lines for the human to add; drop stays candidate-layer-only with memory.md bytes untouched (landed:true, pointing to odo rules retract); apply is the sole held_for_human entry and converges idempotently; daemon and CLI share one actuation path of free-function cores (bug-fix-epoch-40)
- Voluntary drops never feed the freeze set (voluntary is not harmful evidence) and harmful-now promote candidates are rejected with no journal rows at all (bug-fix-epoch-40)
- learning_stall is advisory-only, pinned by tests: it journals once per (hash, stage), never auto-promotes and never auto-drops, and the fold leaves journal event counts and stage states untouched; stall rows render inside the existing LearningPanel candidates feed with no new panels (bug-fix-epoch-40)
- CLI apply accepts a documented risk window: it writes memory.md without holding memMu while the daemon is alive — the same window the rules-retract write-CLI accepts, serialized by WAL busy_timeout with atomic writeFileWithin writes and boot-replayer repair of crash-torn writes (bug-fix-epoch-40)
- Amendment A1 deleted replay checks f/g (a-e, h, and provenance kept; prevented_harm/friction retained as telemetry) and moved efficacy to canary-side f-prime inside learningPromotionVerdict: liveHarm = Live.Rejects + Live.WeakRejects, pass iff liveHarm >= 1, placed after the paired-minimums floor and before the reject-rate leg (bug-fix-epoch-45)
- An f-prime miss does not drop immediately: efficacy_vacuity fires only after a 3-epoch grace keyed by the new stage_epoch measure field (additive, no migration); both new drop exits — efficacy_vacuity (floors met, liveHarm=0, canary age >= 3) and canary_starved (age > 2x stall epochs with floors unmet) — write zero freeze-set entries because vacuity is not harm (bug-fix-epoch-45)
- Pinned seam rule: learningShadowCheckpoints must not flip shadow -> canary when learningCanaryFraction() <= 0, and the stall-advisory dedupe is epoch-keyed so a re-cycled candidate re-advises honestly (bug-fix-epoch-45)
- f-prime's live-harm attribution join uses the same shared predicate implementation as replay-era attribution — single source, no drift; the canary tick fixtures gained live rejects as a baseline correction, not masking (bug-fix-epoch-45)
- R1 rollback is candidate-layer-only (binding user ruling): instant fold to rolled_back with zero memory.md writes; if text had landed via the receipted path, the fold emits the D4 retract receipt and the human resolves via apply_memory / odo rules retract; restore is bounded to the candidate's own delta.add — never opaque/human lines or sibling candidates (bug-fix-epoch-36)
- Two-pass freeze set within a measure tick: rollback (pass-1) folds before promotion (pass-2), closing the same-tick same-text re-entry hole; the lint-side freeze fold strips the oscillation_guard decoration so production journal rows hit the freeze set (bug-fix-epoch-36)
- Lock docs are historical records: coherence fixes are additive banner lines only — original criteria text byte-identical, Status lines immutable, resolution conveyed via a new header line, never inline markup splices (bug-fix-epoch-47)
