# GUI Feature Locks (U1/U2 & Adoption Lock)

- U1 statusbar folding: priority rank ctx-meter → OMP → panel → pipeline/running → finished/bg-runs → count chips, group-atomic; chips are always mounted behind a chip-hidden token (never unmounted — e2e .status-badge hooks stay alive); width is measured on a stable flex container because a content-adaptive row caused a self-lock collapse cascade (bug-fix-epoch-23, bug-fix-epoch-24)
- U1 recovery invariant: a group may only become visible when every higher-priority group is visible (prefix invariant of the lock order); the +N popover rows are built reactively from props, never cloned from DOM (bug-fix-epoch-22)
- U1 remainder: type-token sweep introducing --text-nano, global .truncate cap deleted in favor of explicit max-w-[220px] on slash/mention rows, focus outline kill scoped to .chat-input textarea:focus, toast z-index 90→95 (bug-fix-epoch-23)
- window.setTimeout in SettingsPanel is load-bearing: @types/node makes bare setTimeout resolve to NodeJS.Timeout, breaking useRef<number|null> — reverting reproduces TS2322 (bug-fix-epoch-24)
- U1 landed as cf66635 on its third attempt: first blocked by the supply-chain gate (package.json/lockfile in the diff), second auto-rejected because the vitest gate never ran, third passed all three gates (bug-fix-epoch-23, bug-fix-epoch-25)
- U2 panel overlay: hysteresis state machine evaluating docked-equivalent width (removing the fixed panel widens chat ~420px and a naive reading self-oscillates), callback-ref ResizeObserver because the booted early-return leaves useRef permanently null, offsetWidth instead of clientWidth for border-box correctness, width persistence with empty-string→default guard, and a literal click-through scrim (bug-fix-epoch-25)
- U1 test infrastructure boundaries: the Tailwind vite plugin stubs css?raw to empty under vitest, so CSS assertions read source via node:fs and inject into jsdom CSSOM; jsdom cssstyle does not resolve var(), so token checks assert rule declarations while e2e covers real pixels (bug-fix-epoch-23)
- Adoption Lock P1: ⌘K journal search (typed-only group, ≥2 chars, 250ms debounced cross-project fan-out, one-flight foreign workstream switch via the existing handler with ⌘F prefilled), a data-slot selector map added alongside existing markers with zero rewrites, a table-driven keybind registry with a read-only ⌘/ ShortcutsPanel, unified-diff detection rendering read-only ToolDiffView via DiffViewer's exported parser, and a 10-rule error summarizer producing sticky classified banners while unclassified errors keep legacy behavior (bug-fix-epoch-26)
- Adoption Lock residual scope: JOURNAL_HIT_CAP = 8 caps palette rows (deeper hits remain CLI-only, undecided); P2/P3 items (preview changes, Runs tab, perf harness, theme cascade, esc registry) remain untasked by design (bug-fix-epoch-26)
