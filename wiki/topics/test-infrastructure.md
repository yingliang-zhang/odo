# E2E & Test Infrastructure

- Verify sandbox runs under scratch HOME (credential isolation since P1#11), so any test depending on prefs/HOME must self-isolate via `t.Setenv` — `TestAutoLandStartedRowsAbsentBeforeSpend` was the single un-isolated autoLand case and its latent environmental lie deterministically blocked #39/#40 (main-epoch-25) (main-epoch-16)
- Environment-sensitive test hygiene: `TestPlaywrightBrowsersDir` scrubs `PLAYWRIGHT_BROWSERS_PATH` at start; `go env` subprocesses pinned to `GOTELEMETRYDIR=$TMPDIR/odo-go-telemetry` to kill async-write TempDir cleanup races; fixture `review: rm1@test` single-model configs were migrated to 2-model after the single-judge disarm (main-epoch-26) (main-epoch-15)
- Mock timing defect class: `mock-invoke.ts` evaluated the `fail` branch BEFORE `delayMs`, giving no deterministic failure window (root cause of switch-cache flakes); delay now precedes fail, tests pin windows with `delayMs: 2500`, and a per-workstream `bootstrapLandings` counter proves arm-after-landing (main-epoch-37) (main-epoch-28)
- rAF-race flake class: single-shot assertions racing post-commit effects must become `expect.poll` retries — the contract is "eventually stably visible" (ContextPanel strip-scroll, width assertions) (main-epoch-36) (bug-fix-epoch-7)
- Poll-window convention: poll-loop-dependent assertions carry `POLL = { timeout: 12_000 }` (existing REFRESH family across five specs); RPC-local assertions (textarea, error banner) keep the 5s default — tagging everything would mask mechanism; only genuinely poll-dependent surface tagged (main-epoch-29) (main-epoch-30)
- Keep-alive restructure invalidates page-global text assertions: resolved diffs intentionally render as hidden record cards, so inbox emptiness must be scoped to the `.review-inbox` container (main-epoch-31)
- Stale-test class after semantic flips: M20's `auto_apply` off→main default flip and the `review:` arming gate broke `TestGetSettings`/`TestPhantomDiffVerdictBlocksAutoLand` — stale tests, not production regressions; fixes ride inside the next landing diff's snapshot to avoid separate round-trips (main-epoch-9) (main-epoch-10) (UI-epoch-9)
- Same-bytes reproduction decides flake vs deterministic: re-running the exact patch bytes in a faithful worktree (scratch HOME + filtered env) — deterministic same-bytes failures are real defects (e.g. stale symlink-guard assertion text), unique failures under load are flakes; full-suite reruns of flaky signals carry ~coin-flip re-block risk and no information gain (main-epoch-35) (main-epoch-29) (main-epoch-26)
- Fixture hygiene lessons: extra commits in fixtures must re-read HEAD as BaseSHA (else the freshness ladder fires before the verify gate); e2e seeding via the `fx.ev` fixture seam runs ~18× faster than composer sends; assertions should derive from boot DOM counts, not fixture constants (main-epoch-13) (UI-epoch-12)
- Suite-scale pressure: the daemon's own ipc test suite runs ~450–615s against the default 10m `-timeout`/verify timeout — growth from new boot folds and drains has pushed total runs over the threshold, requiring explicit extended timeouts (main-epoch-42) (bug-fix-epoch-11)
- Operational sabotage: stale Vite dev servers from other worktrees confidently serving old code on :1420 have corrupted two consecutive e2e rounds (`reuseExistingServer` silently reuses them); playwright must run from `gui/` (root resolves vitest) (bug-fix-epoch-7) (bug-fix-epoch-8)
