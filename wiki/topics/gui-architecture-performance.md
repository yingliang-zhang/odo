# GUI Architecture & Performance

- Keep-alive panel contract: the six right-pane panels stay mounted (state preserved) and refetch on inactive→active transitions with uncommitted-draft protection; App passes an active prop (main-epoch-34)
- Quiet ticks re-render nothing: memo on keep-alive panels plus prev-bail comparators on aggregate setState, diffs semantic equality, and the hidden Ledger memo keyed to the last relevant review/memory event seq (main-epoch-30, main-epoch-32, main-epoch-34)
- Transcript windowing: tail window of 25 run groups as a render-only cut (all folds and ⌘F search read the full events array), stepwise 25-group expansion via a fold chip, search force-reveal by seq-membership scan; live run and preview bubble can never be windowed out (UI-epoch-12)
- Pointer-move resize commits at most one width per animation frame via rAF buffering; the final width commits synchronously on pointerup (UI-epoch-12)
- Sidebar collapse animates transform + label fade only; the 240→48 width/padding transition was removed so geometry flips in a single relayout, with dual keyframe names so mid-animation re-toggle restarts (UI-epoch-12)
- Poll recovery clears only 'poll failed:'-prefixed errors; switch/send errors persist until the 10s ERROR_BANNER_MS timer — fixing the silent switch-rollback flake class (main-epoch-38)
- Visual incoherence root cause is documented: app.css:56 mixes Apple HIG radii, Multica type scale, Hermes stroke/shadow and Apple motion while tailwind.css sits mid-migration with legacy overrides winning the cascade; remediation order is perf fixes → durable pages vs working panes → single design contract → chat-column 760–840px typographic pass (main-epoch-42)
- Outstanding perf debt: main bundle 625KB over the 500KB threshold, act() warning noise, durable-pages information architecture, and design-contract consolidation remain unscheduled; the rAF resize claim is structural (cadence parity), not a measured device speedup (main-epoch-42, UI-epoch-12)
