# MoA Chain: Resilience, Receipts, Design-MoA

- R-W1 moa client: bounded retry (max 3, 200ms×2ⁿ ±50% jitter, Retry-After honored capped 30s), typed `Error{Status,Class,RetryAfter}` (rate_limit/server_error/network/client_error/timeout), no retry on caller cancel; `math/rand/v2` per project rule (moa-chain-epoch-1)
- R-W1 usage ledger: `InputTokens/StopReason/WallSeconds/TokPerSec` — tokens record final request, wall time covers whole logical call; retries/escalations folded (moa-chain-epoch-1)
- R-W1.5 premise correction: R-W1 (`e2f8b61`) added only retries/typed errors/usage — `RequestSHA16/RequestBytes` did NOT exist and were built fresh, not just journaled through (daemon-misc-epoch-2)
- Receipt convention: recorded at `post()` marshal point (only place wire bytes visible); one receipt per retry chain (byte-identical resend), escalation receipts point to the final body, error returns carry none (daemon-misc-epoch-1, daemon-misc-epoch-2)
- sha16 helper duplicated in moa package (import cycle with ipc); 'convention is the contract' comment, same sha256-prefix scheme (daemon-misc-epoch-2)
- Audit of every `moa.Query*` call site in internal/ipc confirmed journal receipts at all five lanes (/panel, /vision, manual review, auto-land gate, skill gate) — 'no residual' verdict (daemon-misc-epoch-1)
- R-W3: learner/curator migrated behind `learner_via`/`curator_via` flags via shared `resolveVia(task,prefKey)`; runMoaOneShot truncation fail-closed; ADR-0003 inv7 reworded (direct-moa transport creates no model write path); GUI `DISTILL_READ_TIMEOUT` 2100→3300s, `CURATE_READ_TIMEOUT` 660→1560s from 1446s worst-case moa chain; both flags ship opt-in, no decision to flip defaults (moa-chain-epoch-1)
- R-W4 Design-MoA: 3 blind legs (`QueryWithTools`, prefs `review:` models) + consolidator 4th `moa.Query`, gated `design_via: moa` fail-closed on absent/omp/garbage; `designing` single-flight mirrors `curating` precedent (moa-chain-epoch-2)
- Truncation semantics: truncated leg = failed leg — text dropped, receipts kept, pipeline degrades; only ALL-legs-truncate fails; truncated consolidator fails closed with no `design_lock` row (moa-chain-epoch-2, moa-chain-epoch-3)
- Wire key kept as `design_proposals` over instructed `proposals`: encoding/json dominant-field rule would silently corrupt memory_proposals Responses — pinned by `TestDesignMoaResponseWireKeys` (moa-chain-epoch-3)
- Leg labels by prefs index `'A'+i` so gaps persist when a mid leg drops; dropped legs named in consolidator prompt but text excluded (guards invisible vote); failure markers attach per-leg metadata + consolidator receipt via shared `designConsolidatorReceipt` helper (moa-chain-epoch-3)
- Symlink containment: mirror `fsToolExecutor.resolve` — EvalSymlinks root once (avoids macOS /var→/private/var false positive), lexical check per file, escapers rejected pre-fanout (moa-chain-epoch-3)
- `/panel` has no consolidator (`handlePanelQuery` concatenates legs verbatim); consolidator pattern available in `design_moa.go`; user decision pending: default-on vs pref-gated (UI-epoch-2)
