# Diff Adjudication & Restart Recovery

- Restart re-fire root cause: `NewServer`'s `recover-pending-diffs` called `ListAllPendingDiffs` (project-wide, all workstreams) and unconditionally re-fired `maybeAutoLand` for every pending diff — every restart re-ran verify (≤10 min) plus panel (measured ~4–24 min) per unresolved diff; idempotent but pure waste (UI-epoch-9)
- Plan A dedup implemented: `recoverPendingDiffs`/`strandedPendingDiffs`/`pipelineTerminalDiffIDs` — skip diffs with terminal rows (`auto_land_blocked` with reason≠`panel_infra`, `moa_review{actor:auto_panel}`, `auto_revise_round`); still re-fire breadcrumbs (`auto_land_started`/`refresh_attempted`), `panel_infra` (designed retry channel — infra failure is not a verdict), and human `moa_review`; journal read failure fail-closes the whole recovery pass (UI-epoch-9)
- Deployment sequencing note: the dedup fix itself requires a daemon restart that triggers one final old-logic full re-fire — rejecting diff #21 in the GUI before restarting saves a whole panel round (UI-epoch-9)
- #21 was rejected directly via the daemon socket (`reject_diff` → store `rejected`) because the GUI was unclickable due to the very lock bug being fixed; daemon-side `handleDiffAction` has no pipeline lock (only acceptMu + status checks), so human reject always passes (UI-epoch-10)
- #22→#25 ordering: #22's `verify_failed` was a stale pre-rebase verdict (snapshot rebased byte-identical with worktree staging, main green after landing); #25's block was `protected_path` policy, not quality; disjoint `server.go` regions made the order safe; #25's dedup went live self-evidenced by `recover-pending-diffs: 1/1 pending diffs already adjudicated — skipping their re-fire` (UI-epoch-10)
- GUI review-lock root cause: pure-bookkeeping `auto_revise_product` journal rows (drainRun Fix B1, lets `supersedeChain` find the repair product) were treated as pipeline state by `derivePipelineStates.latestChainRow` → `default` branch → `in_flight` → permanently locked Accept/Reject; fix skips such rows so the origin falls back to the round row's `revise` phase and stays human-decidable (UI-epoch-10)
- The lock fix became diff #26 (pipeline.ts skip + typed `product_diff_id` + regression test replicating the real 21→22 chain asserting 21=revise, 22=blocked, no locks): accepted and verified on main — 15/15 file tests, 110/110 full suite (UI-epoch-11)
- Human-button lock truth table: `in_flight` (verify/panel/refresh) and `landing` lock review buttons; `queued`, `revise`, `blocked`, `suspended` stay usable because those are the states where human action is the escape hatch; scope is active-conversation-only by design (UI-epoch-4)
- Diff 9 lifecycle: panel `needs_fixes` on 3 real defects with auto-repair blocked (85 KB > 64 KB `repair_prompt_too_large` cap) → rejected into a manual fix round → requeued as diff #11, `protected_path`-blocked by design (touched `autoland.go`), landed as `d709366` after manual accept (UI-epoch-4) (UI-epoch-5) (UI-epoch-6)
- #33 was never "reappearing" — it had sat `pending` since a prior `verify_failed` and was simply never disposed; it was user-rejected as redundant because #34 is a strict superset (15 shared files, 0 files unique to #33) (main-epoch-17)
- #34 was re-fired via a self-daemonizing restart watcher (`panel_infra` is the only non-terminal blocked reason; restart re-fire is its designed retry) only after model token refresh — both review models had returned HTTP 401 and the pipeline correctly fail-closed (infra failure ≠ verdict → block, not pass); it then auto-rejected via `panel_mixed` (kimi-k3 accept, glm-5.2 accept, deepseek-v4-flash reject on the false objective mismatch) with the mechanical classifier tagging `risk_class:["destructive"]` (main-epoch-17)
- Open: the +202 KB P1 #10–#14 batch (real defect fixes: shared client, verify sandbox, recover-pending-diffs excluding loop seed) exists only at `.odo/diffs/6a89261c-d0af402d09cc.diff` and is committed nowhere — pending user decision between `git apply` onto ba68fb5 (optionally auditing deepseek's three objections first) or abandoning (main-epoch-17)
