# GUI Rendering & State

- Keep-alive: six panels are memo'd components kept mounted while hidden, refetching on inactive->active transitions with uncommitted-draft protection; keep-alive hides records but leaves them mounted, which page-global test assertions must account for (main-epoch-32) (main-epoch-34) (main-epoch-31)
- The pipeline-state memo is keyed on the last relevant review/memory event seq so hidden panels (e.g. Ledger) don't re-derive on every events-array replacement (main-epoch-34)
- The 350ms full-App re-render was closed by diffs semantic-equality checks, a memo'd ChatSurface, and freezing eight unstable props — quiet ticks now produce zero re-renders (main-epoch-30)
- Switching is stale-while-revalidate: a per-conversation LRU journal cache renders instantly and merges by seq (append-only journal makes seq-merge lossless); bootstrap passes afterSeq/conversationId for incremental replay; daemon SQL/file I/O moved outside s.mu; generateAgentsMD skips writes when content is identical (UI-epoch-8)
- Optimistic switch flips roll back via a rootFlipped boolean computed at flip time (no closure comparison), and in-flight handleSend responses are guarded by captured (cid, root) so responses can't land in the wrong conversation (UI-epoch-8)
- Advisory slash submits (/panel, /vision, /preview) are detached from the composer await: the box clears and unlocks immediately, the background promise drives the spinner, and instant daemon rejections restore the draft — accepted semantic change that normal sends during a read-only panel consult are now possible (UI-epoch-3) (bug-fix-epoch-3)
- panelThinking is a counter (not boolean) so concurrent advisories share the spinner with last-out-turns-off; late advisory rejections restore the draft only if the composer is untouched, otherwise keeping user content plus an error banner (bug-fix-epoch-4)
- Panel progress is memory-only (never journaled): panelProg tracks N/M legs per panel, and after a concurrent-panel corruption it became a per-consult batch-group slice whose defer removes only its own batch (bug-fix-epoch-4) (main-epoch-38)
- twMerge treats any bg-* class as the background-color group: an inert e2e marker class (bg-runs-menu) silently discarded popover backgrounds to rgba(0,0,0,0) — marker classes must never carry utility prefixes (UI-epoch-7)
- Human review buttons lock by a pipeline-phase truth table: in_flight/landing lock while queued/revise/blocked/suspended stay usable (human action is the escape hatch there), active-conversation-only; auto_revise_product bookkeeping rows are excluded from latestChainRow so revise products can't permanently lock the origin (UI-epoch-4) (UI-epoch-10)
- The steer queue is a journal-only derivation (user_message{steer:true} minus run_prompt.steer_seqs minus steer_dropped closures); the daemon enforces fail-closed orphan rejection, seq-carrying receipt lines, and drop_queued_steer so every journaled steer ends consumed or dropped (UI-epoch-4) (UI-epoch-5)
- Successful polls only clear 'poll failed:'-prefixed errors; switch/send errors persist until the 10s ERROR_BANNER_MS timer — fixing the Playwright flake class where switch failures were silently rolled back (main-epoch-38)
- IME stuck-composition fix: WKWebView Enter can submit mid-composition so compositionend never arrives and composingRef sticks forever (permanent blue marked text) — submitDraft now synchronously clears the textarea and composingRef on send (UI-epoch-4)
- GUI roadmap partial delivery: semantic equality + memo and keep-alive tabs done; true windowed lists, tab consolidation (6 to 3-4 + More), composer toolbar merge, status-bar noise reduction, visual rework, and WKWebView perf work remain unstarted; main JS chunk still ~625KB with lazy split unscheduled (main-epoch-30) (main-epoch-38)
