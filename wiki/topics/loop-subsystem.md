# /loop Subsystem (M19)

- docs/design/loop-design-lock.md (C1–C12 + V1–V13) is the authoritative contract; settle.go's auto-revise ladder is the Mode A architectural template; loop id is the started journal row's seq, not a payload field; loop-generated accepts are excluded from autonomy human-accept metrics via an explicit loopActor filter (main-epoch-1) (main-epoch-2)
- Audit base comes only from SEED pending diffs or an explicit base= arg — no history auto-derivation; fixtures failing with nothing_to_audit were fixed by passing base=<frozen base sha> explicitly, not by changing the preflight (main-epoch-2)
- Audit subject cap raised 64KB→256KB (loopAuditSubjectCapBytes, loop-owned) because auto-land squashes each accepted diff into one commit (M19 impl = 233,533B, exceeding 64KB); the findings-feed breaker stays on settleCommentsCapBytes 16KB and >256KB remains a hard wall with cause subject_too_large; all other convergence guards unchanged (severity gate, infra-is-never-a-verdict, stall, budget breakers, round cap) (main-epoch-5)
- P1 fixes: /loop added to audit slash mirror lists; adapters registry guarded by adaptersMu with all reads migrated to adapterFor/adapterNamed and runMeta.adapter set; loopRowSpend JSON round-trips the row before extraction (journaled concrete slices never satisfy []interface{} assertion); human send mid-loop run passes the concurrency check, cancels the loop run, and suspends human_interleave — reinforced by the loopDrainActive fold guard so a canceled run's drain can't clobber the suspend row (main-epoch-3)
- P2 fixes: tests pin the four security gates (protected_path, supply_chain, stall, budget overflow + resume budget= recovery); loopStartAudit preflight-refuses auto_apply != main (parity with Mode B); C10 admission fold moved inside the mu critical section (TOCTOU closed structurally, no deterministic test possible); loopLeadingFlags parses only leading k=v tokens; loopSeed re-folds between diffs honoring pending stop/suspend; human reject inside the restart window records loop_task_done{status:skipped} (main-epoch-3)
- Daemon fold semantics: loop_started carries loop_id:0; loop_budget_exceeded folds to suspended/budget_exceeded; review_action{accept|auto_land_blocked, actor:auto_loop} closes fix phases; notifiedKinds dedups notification receipts and first journaled row for a terminal kind prevents re-fires; loop_completed carries rounds and findings_count (main-epoch-4)
- GUI wave: loop_event rows render as one compact bookkeeping bubble (never agent text, survive distill fold); LoopChip is a pure TS mirror of the Go fold re-deriving from the event stream (loops continue daemon-side while GUI closed); slash registry entries gain description with full-list-on-'/' + keyboard model + background-pill token overlay; notifications reuse the already-installed @tauri-apps/plugin-notification with receipts journaled via loop_ctl{action:notified} — honest limit: daemon never talks to the OS, firing requires the GUI open (main-epoch-4)
