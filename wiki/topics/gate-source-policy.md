# Gate-Source & Protected-Path Policy

- Invariant 1 (doctrine since diff #27): agents never write memory — `.odo/` and `wiki/` hunks hard-blocked pre-panel in `autoLandCheck` AND at accept-time by `rejectMemoryPaths` for every actor including a human Accept click (main-epoch-24)
- Wiki content lands only through the daemon's own distill/`commitWiki` pipeline; agent diffs carrying wiki hunks are misrouted and must be stripped and resent (epoch-42 diff fully rejected for touching `wiki/topics/memory-distill.md`; wiki corrections wait for daemon distill) (main-epoch-24) (main-epoch-42)
- Supply-chain paths (lockfiles, manifests, `.github/workflows/`, `.gitlab-ci.yml`) stay hard-blocked — a one-line dependency change is an RCE diff review structurally cannot judge; `autoLandSupplyChainPath` SSOT predicate shared by gate and risk classifier (main-epoch-24) (main-epoch-23)
- Hand-written 10-file `protectedGateFiles` map replaced by structural two-tier policy (`internal/ipc/gatepolicy.go`): Tier-0 = gatepolicy.go + `gate_manifest.json` (human-only, editing IS the exemption), Tier-1 = prefixes `internal/ipc|store|git|moa|adapter/` + root main.go; matching is case-folded (bug-fix-epoch-14)
- Startup drift latch: sha16 of each Tier-0 file recomputed in `NewServer`; mismatch/missing journals `memory_update{cause:"gate_source_drift"}` and latches `s.gateDrift`, causing autoLand/loopFixPipeline/settle majority valve to refuse `gate_policy_drift` fail-closed; human-only `odo gate re-pin` restores (bug-fix-epoch-14)
- Accept-time guard: Tier-0 hit + any non-human actor → hard error naming the file; Tier-1 hit without panel attestation → block; `git.PatchPaths` includes pre-image paths so a rename out of a protected prefix is gate-source on both sides (bug-fix-epoch-14)
- C0 purity: `autonomy.go classifyDiff` stays memory-prefix-only — gate-source hits must not be folded into the autonomy ladder (bug-fix-epoch-14)
- Mode A loop-fix reroute classification order: memory prefix → Tier-0 (suspend `loop_suspended{cause:"risk:gate_core"}`) → supply-chain → Tier-1 via `s.autoLand` verbatim; the verify-only loop landing path was deleted; fold attribution is fail-closed on `loop_diff_bound` binding (bug-fix-epoch-14)
