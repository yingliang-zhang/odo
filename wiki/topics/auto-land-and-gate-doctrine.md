# Auto-Land Pipeline & Gate Doctrine

- Stage visibility: daemon journals `auto_land_started{stage:verify|panel}` before `runVerifyGate` and before `reviewFanout` (reusing `review_action` rows, no new event types); rows carry `patch_sha16`, are excluded from the distill prompt via `foldExcludedReviewAction`, produce no transcript bubble, and unknown stage values degrade to generic in-flight for forward compatibility (bug-fix-epoch-1)
- Stage visibility is honest about free gates: mechanical gates (`run_errored`, `protected_path`, `base_stale`) return in milliseconds with zero started rows, so the chip goes queued→blocked directly and never claims "running"; the refresh probe precedes `started(verify)` so rebase scenarios keep their in-flight hint (bug-fix-epoch-1)
- Protected-gate self-modification rule: nine `protectedGateFiles` (incl. `internal/ipc/autoland.go`) flatly block auto-land even on good diffs; the designed escape hatch is human accept — `review_action{action:accept}` with no actor field vs `actor:auto_panel` on auto-lands, pinned by `TestHumanAcceptGateSourceAllowed` (bug-fix-epoch-1)
- Policy: keep the protected surface unchanged (loosening would let the pipeline self-modify unsupervised); mitigation for review fatigue is batching protected-file changes into larger single diffs — the spike in manual gates came from steer-queue/pipeline self-modification diffs touching `autoland.go` (UI-epoch-6)
- Verify-gate environmental false positive fixed at the root: worktrees contain no `gui/node_modules`, so diff 8's `npx tsc` verify failed in 6 s while content was clean (102/102 e2e after manual symlink); `runVerifyGate` now auto-provisions `gui/node_modules` by symlinking from the main checkout at both call sites (autoLand and loop-fix), pinned by three Go subcases (UI-epoch-4)
- Doctrine redesign (zero-manual-lock, diff #27, main `5ec522b`): `autoLandCheck` is three-valued — gate-source edits, new top-level dirs, and net-new assertions downgrade from hard block to panel risk annotation; hard blocks remain only for memory and supply-chain paths; new execution-layer evidence gate `panelVerdictAttestsDiff` binds the panel verdict to the exact landing bytes via `patch_sha16`; `rejectProtectedPaths`/`rejectExecutorPaths` deleted with zero residue (UI-epoch-11)
- Doctrine origin and loss: user policy declared human review low-value and future reviews fully automatic; the first implementation was green but lost when diff #25's accept wiped the worktrees pre-registration — it was rebuilt by replaying 24 verbatim write/edit payloads from the journal (UI session seq 3933–4204) because the session transcript had been pruned, landing with zero drift (UI-epoch-10) (UI-epoch-11)
- Doctrine was deployed behaviorally live, not source-only: rebuilt binaries atomically swapped onto both the odo daemon and the hermes-agent daemon; hermes-agent's accumulated pending diff stayed in its own store (`~/.hermes/.../journal.sqlite` + capture file), decoupled from odo's binary swap and daemon lifecycle (UI-epoch-11)
- Known systemic bug remaining: FIXED in `abca6f1` (schema v3, stored `diffs.goal`) — the review prompt anchored the objective to the session's last user message instead of the diff's provenance; this produced diff #34's false "objective mismatch" auto-reject (main-epoch-17 → main-epoch-18)
- Divergence doctrine (2026-08-22 user directive, post-#34): a SPLIT panel no longer auto-rejects — `panel_mixed` instant-kill is retired. Every divergent verdict (mixed reject OR needs_fixes) enters the revise ladder (≤3 rounds); the cap evaluation resolves by STRICT MAJORITY ("按照超过半数情况决定"): accepts > N/2 lands {majority_accept}, rejects > N/2 auto-rejects {panel_majority_reject} with advisory + chain supersede, no strict majority (e.g. 1/1/1) suspends for the human; gate-source diffs stay excluded from the whole majority decision (the 2026-08-22 security cut, generalized) (main-epoch-19)
- #34 ledger divergence closed by append, not repair: journal row `review_action{action:"ledger_correction"}` at conversation 1 seq 8017 (2026-08-22) records that #34 stands terminal-rejected in the journal while its content landed in main via `a5c98ca`+`abca6f1`; the rejected status was deliberately left unrepaired (main-epoch-19)
