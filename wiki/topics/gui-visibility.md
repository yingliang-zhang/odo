# GUI Structure & Visibility

- Message stream rework: agent bubble `bg-raised`+hairline+radius 12/12/12/4 mirrored against user, hover copy + hover timestamp strip, lone tool call inline vs burst fold 'N tool calls · name(args…)', preview as bubble+caret, enabled 1100px `--chat-column-width` (UI-epoch-1)
- Popover transparency root cause: twMerge treats ANY `bg-*` class as the background group — e2e marker class `bg-runs-menu` silently discarded the real `bg-[var(--bg-elevated)]` on all 5 StatusBar popovers; never put `bg-`-prefixed markers into PopoverContent (UI-epoch-7)
- Status-bar icon stacking: Tailwind preflight sets all svg `display:block`; in non-flex spans icons take their own line — fix `inline-flex items-center gap-*`, wrap text in `min-w-0 truncate` child for ellipsis (UI-epoch-3)
- Pipeline stage visibility: daemon journals `auto_land_started{stage:verify|panel}` before verify/panel; chip shows 'verify running…'/'panel reviewing…'; started rows excluded from distill; free gates (protected path/base_stale) produce zero started rows → chip goes queued→blocked honestly (bug-fix-epoch-1)
- Stuck-pipeline diagnostic: truly stuck = no moa/auto-land log lines + no review_action + >~25min (10min verify + 900s/leg panel); a bare 'queued' lasting seconds is now anomalous (bug-fix-epoch-1)
- `pipelineHumanLocked` truth table: `in_flight`/`landing` lock human buttons; `queued`/`revise`/`blocked`/`suspended` stay usable (human is the escape hatch); active-conversation scope only (UI-epoch-4)
- Permanent-lock root cause: `latestChainRow` treated bookkeeping `auto_revise_product` rows as state → in_flight → locked; daemon semantics are 'revise done, product self-manages' — GUI now skips such rows, origin falls back to round row (UI-epoch-10, UI-epoch-11)
- Context-meter: SVG ring ok/warn/err at 50/80 thresholds, honest derivation (wall time + in/out bytes; tok/s only when counts exist); `total_prompt_bytes` reverse-scanned from latest user_message/run_prompt closure — zero new IPC (gui-wave-epoch-2)
- LedgerPanel renders review_action receipts with severity ramp (critical red → high amber #d19a4a → medium warn → low link → gray; `risk-unrated` dashed-neutral never masquerades as clean); unknown class → forward-compat neutral (gui-wave-epoch-1)
- Sidebar orders Needs-input → Working → Idle (stable sort keeps daemon created_at); 'Done' tier deliberately unimplemented — no done-observable in pending_counts+running_workstreams (daemon task registry is the prerequisite, open loop); `timed_out` chip is defensive — daemon does not journal it yet (gui-wave-epoch-1)
- Sidebar/TopBar: right-sidebar close X replaced by TopBar toggle mirroring left pattern; ⌘J unchanged (UI-epoch-7)
