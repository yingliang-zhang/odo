# Panel & Advisory Slash Commands

- /panel runs synchronously inside the daemon send_message RPC (handlePanelQuery): persists the user message, fans out to 3 MoA legs (kimi-k3 / glm-5.2 / deepseek-v4-flash) with ≤16 tool-loop rounds each, and journals one merged answer only after all legs finish — minute-level chat silence is by design (900s HTTP floor ×3 retries, worst ~24min/request) (main-epoch-8) (bug-fix-epoch-3)
- Composer freeze root cause: ChatSurface held sending=true across await onSend and sending lives on the single global instance, locking ALL workstream composers for the whole consult; fix detaches advisory submits (box clears and unlocks immediately, draft restored only on instant daemon rejection) — accepting the semantic change that users can now send normal messages while a panel consult is in flight (bug-fix-epoch-3)
- Advisory submits preserve the one-shot park arm (daemon routes slash commands before steer/park so park is never queue-consumed; GUI clearComposer keepPark:true); late advisory rejection restores the draft only if the composer is untouched, otherwise it keeps user content and shows only the error banner; panelThinking is a counter, not a boolean, so concurrent advisories share the spinner (bug-fix-epoch-4)
- Whitespace parity between daemon and GUI routing is 'verified-correct-but-unpinned': HasPrefix(rest, " ") is literal space only — /panel⏎, tab, '/ panel' fall through on both sides; locked with exact source citations + 17 daemon-mirror unit cases rather than a behavior change (bug-fix-epoch-4)
- Slash-activity visibility reuses existing channels (zero new RPC): conversations with slashing[conv]>0 count into running_workstreams (display-only blue/purple dot + StatusBar bg chip); PanelProgress.legs[] registers model names at fan-out in prefs order with per-leg done/error, and the poll path deep-copies (shallow Legs slice = encoding race); GUI replaces the spinner row with a per-leg status card (main-epoch-8)
- Panel answers are raw per-leg concatenation with no consolidator (pre-existing open item); /vision and /preview get the activity dot but no card (not panelProg) (main-epoch-8)
- No cross-project /panel leak is possible: the bridge connects per projectRoot to that project's .odo/odo.sock with a fresh connection per call, a stray send during an active run is rejected ('agent already running'), and applyBootstrap fully replaces events on project switch; the perceived leak was both projects having a workstream named main with no project name in the old TopBar; the same investigation attributed all-EOF legs to a coding.sudoai.cc network failure, not routing (main-epoch-7)
