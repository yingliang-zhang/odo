# Panel Review Semantics and Adjudication Rules

- Three-round rule: rounds 1–2 require unanimous accept (fail-closed veto retained); round 3 (terminal, settleMaxReviseRounds 3→2) may land on the majority valve — ≥2/3 accept + zero reject + zero infra + zero truncated — persisting majority_accept evidence (main-epoch-21)
- The majority valve's only reachable composition is {2 accept, 1 needs_fixes}: a reject leg auto-rejects before the ladder and an infra leg blocks before evaluation (main-epoch-21)
- Truncated legs are forced to needs_fixes at reviewVerdict construction (server.go:3251); the round-3 valve additionally fails closed on any truncated leg — relaxing this needs an explicit user decision (main-epoch-21) (main-epoch-20)
- Gate-source diffs (protectedGateFiles: autoland, autonomy, risk, contradiction, settle, learner, review, ledger, design_moa) require UNANIMOUS panel attestation since 2026-08-22 — the majority valve has no attestation power; human accept is the escape hatch pinned by TestHumanAcceptGateSourceAllowed (main-epoch-24) (bug-fix-epoch-1)
- The execution-layer evidence gate panelVerdictAttestsDiff binds the panel verdict to exact landing bytes via patch_sha16 (diff #27 doctrine, landed 5ec522b) (UI-epoch-11)
- Diff #36 diagnosis: panel mechanics were fully healthy (verify passed, 3 legs voted, zero infra errors); the 2:1 split auto-rejected under the live stale binary's one-vote-veto rule — daemon ran 5ec522bd+dirty with schema_version=2 because epoch-20's 'new binary' was never swapped in (main-epoch-21)
- kimi leg objections were adjudicated as substantive governance items, not malfunction: majority restricted to the terminal round only, and truncated legs invalidate the valve (main-epoch-21)
- Panel Q&A tolerates leg death: a deepseek leg failing on 'tool loop exceeded 16 rounds' contributed zero information but the panel proceeded on surviving legs (UI-epoch-2)
- Panel-leg deadlines exist (P1#9): per-leg outer ceiling is TimeoutForModel = 1446s (~35 min/phase); the old 'stuck ~61 min' figure was pre-deadline escalation-stack residue and was NOT written into docs to avoid a second stale contract sentence (main-epoch-23)
- Third-party review adjudication discipline: 4 reported findings verified individually — 3 confirmed by experiment, the GUI test failure reproduced but misattributed (test defect, not app bug) — then all four fixed in one 7-file +431/−19 pass (main-epoch-28)
