# Protected Paths, Risk Gates, and Memory-Write Doctrine

- Hard blocks are minimal: .odo/ and wiki/ memory paths + supply-chain manifests/lockfiles (post-5ec522b narrowing); gate-source files are panel risk annotations requiring unanimous attestation, not blockers (main-epoch-24)
- rejectMemoryPaths backstop at accept refuses memory paths for EVERY actor including a human Accept click — invariant 1: agents never write memory; wiki content routes only via distill/commitWiki auto-commit (main-epoch-24)
- Diff #39 added registration-time refusal for memory-path diffs (drainRun guard after ExtractDiff, before InsertDiff, reusing the same SSOT predicate): transcript advisory naming paths + distill route, salvage patch kept in .odo/diffs/, immediate worktree retire, run_tainted ledger with refusalDetail — no diff row, so GUI inbox/badge stays clean (main-epoch-24)
- Decision: fail fast at registration, do NOT narrow autoLandCheck — downgrading only the pre-panel layer wastes panel spend (executor still refuses); opening both layers deletes invariant 1 and reopens prompt-injection → correlated 3-leg failure → persistent memory pollution; a one-line lockfile change is an RCE vector diff review cannot judge (main-epoch-24)
- Diff #38 was the motivating case: a 20-file P2 diff with a wiki/ hunk + 4 gate files was permanently unlandable — rejected for ledger cleanup (seq 9964); the append-only archive retains its patch (main-epoch-24)
- Registration-time refusal takes effect only after #39 lands AND the daemon restarts — the running daemon still uses the old binary image (main-epoch-24)
- Dirty-path accept guards: new git.DirtyPaths precheck at both apply sites (accept + stale-base refresh) refuses when patch-own paths carry staged/unstaged/untracked content — rollback previously wiped user unstaged AND staged content even though git apply --3way atomically refused; refusal names paths, keeps diff pending, journals refresh_attempted{dirty_refusal} on the stale path, deliberately NOT wrapped as errBaseStale (that would burn ~8.5-min verify rounds on dirt requiring human triage) (main-epoch-28)
- Ordering preserved: ProbeAlreadyLanded identical-content rescue runs before the dirty refusal — unstaged identical edits still land via bookkeeping (main-epoch-28)
- retireRun now retires the run matching the diff's own worktreePath; the byConv binding is only a fallback — previously reviewing an older diff could close a newer run's worktree/session or kill an in-flight auto-land verify, causing spurious verify_failed auto-rejects (main-epoch-28)
- Risk classifier hardening: env-dump probes (os.Environ forms via riskEnvDumpTokens), split rm flags (rm -r -f, -Rf, --recursive --force via FieldsFunc tokenization), CI workflow paths via the shared autoLandSupplyChainPath SSOT predicate used by both gate and classifier (main-epoch-23)
- Prompt-token breaker CJK margin fixed: estimatePromptTokens is now ASCII/4 + 1 per non-ASCII rune (len/4 underestimated ~3×; pinned 85K old-pass vs 110K new-block); loop spawn prompt_tokens_est still uses chars/4 — separate-diff open item (main-epoch-23)
