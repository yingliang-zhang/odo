# GUI Status Surfaces: Ledger Receipts, Status Bar, Background Runs

- LedgerPanel renders review_action journal rows as receipt cells (zero new IPC, data from existing events state): actor badge (auto_panel→Auto vs human), risk severity ramp (credential_probe=critical red → data_exfil/destructive amber → security_weakening yellow → supply_chain blue → clean), timed-out chip, blocked/refresh detail; unrated is dashed-neutral — never masquerades as clean (gui-wave-epoch-1)
- Background-run visibility derived from pending_counts + running_workstreams (no daemon task registry yet): multi-target status-bar dropdown, raw-set watch (not the view-filtered set) so jumping to a bg run can't trigger a false 'finished'; bg rows honestly show only 'still running'; the 'Done' tier is deferred to the daemon wave (gui-wave-epoch-1)
- Honest-derivation rules for Wave B surfaces: prompt bytes reverse-scanned from journaled user_message/run_prompt rows; wall time + in/out bytes shown byte-form unless daemon supplies tokens (defensive input/output_tokens auto-upgrade); receipt per-layer breakdown lists verbatim sha16 keys only — nothing fabricated (gui-wave-epoch-2)
- Billed usage (real tok/s) still needs a daemon wave: the adapter must parse OMP usage at message_end and journal it into agent_done; the GUI is already ready (gui-wave-epoch-2)
- Right-sidebar close X replaced by a TopBar toggle mirroring the left-sidebar pattern (⌘J shortcut unchanged) — one convention for both rails (UI-epoch-7)
- Status-bar svg stacking bug: Tailwind preflight display:block on svg stacks a spinner above its text in non-flex chips; fixed with inline-flex items-center gap per the STATUS_BADGE convention — same fix applied to the path-copy Check icon victim (UI-epoch-3)
- Fixture placement lesson: review_action fixture rows placed in conv 1 broke 3 existing assertions (MessageBubble renders them as chat bubbles, badge-accept collided with getByText('Diff #2')); moved to conv 3 (gui-wave-epoch-1)
