# Auto-Land Pipeline Mechanics and Blocked-Diff Recovery

- Pipeline: run-end drainRun extracts diff → verify (~8.5 min full-package suite) → 3-model panel → auto-land; terminal blocks journaled as auto_land_blocked{reason, diff_id, actor:auto_panel} with reason ≠ panel_infra non-replayable on restart (autoland.go:146-156) (main-epoch-25)
- Human Accept is the only unlock from terminal blocked states; accept_diff IPC is hard-guarded on Status==DiffPending (server.go:2745), so a panel_mixed auto-reject permanently closes the accept channel (main-epoch-27)
- Recovery precedent: manual git apply + commit + journal review_action ledger_correction row (corrects_seq + landed_commits); store status deliberately left 'rejected' — correction, not silent DB mutation (main-epoch-27)
- #41 landed manually as cf09b9e after all three glm rejection reasons were code-audited and refuted (9 capDetail callsites tail-only, playwright empty-var equivalence, GOTELEMETRYDIR default-dir design); rerun had zero information gain and re-risked a fabricated rejection killing the accept channel (main-epoch-27)
- #42 accepted manually (seq 11202) → landed 8a16ba5: a mid-panel user restart ate all leg verdicts, and the re-verify failure on identical bytes was a load flake in one spec (6/6 isolated, 5/5 repeat-each) while the patch touched no advisory/composer paths; rerun also re-exposed the #41 fabricated-rejection risk (main-epoch-29)
- Before manually accepting a pre-panel diff, regenerating the patch file per drain semantics (git add -A && git diff --cached HEAD) is safe — no patch_sha16 attestation binding exists yet (#37 landed as 1203d93 with the coupled ledger.spec.ts fixes included) (main-epoch-22)
- Daemon-restart recovery (diff #25, landed) dedups via pipelineTerminalDiffIDs: auto_land_blocked (non-infra), moa_review{actor:auto_panel}, and auto_revise_round rows are terminal → skipped; auto_land_started/refresh breadcrumbs, panel_infra, and human-only diffs re-fire as genuinely stranded; journal read failure abandons the whole pass fail-closed (UI-epoch-9)
- Stage visibility (diff #6, c28a136): daemon journals auto_land_started{stage:verify|panel} before runVerifyGate and reviewFanout; free gates (protected_path, base_stale) emit zero started rows so the chip honestly goes queued→blocked; rows are excluded from distill prompts and have no transcript bubble; true stuck = no journal rows + >~25 min (bug-fix-epoch-1)
- capDetail moved from head-trim to tail-bias (`…[earlier truncated]\n` prefix, rune-safe cut, 4KB cap) because go test's --- FAIL lines live at the tail and verify_failed blocked rows previously never named the failing test — recoverable only by reproduction (main-epoch-26)
- #40 accepted after a faithful verify reproduction (same byte set 2ca27a8+#40, scratch HOME + allowlist env, 499s) proved the sole failure was the pre-existing deterministic TestPlaywrightBrowsersDir env defect, not #40's content (main-epoch-26)
- GUI-side pipeline-lock misalignment: auto_revise_product rows are pure bookkeeping, but latestChainRow treated them as state → permanent pipelineHumanLocked; fix skips those rows so the origin falls back to revise phase and stays human-decidable (UI-epoch-10)
- advisory-slash.spec.ts robustness change (POLL timeout) is left in the worktree pending run-end drain → verify (tsc + full 117 playwright) + panel (main-epoch-29)
