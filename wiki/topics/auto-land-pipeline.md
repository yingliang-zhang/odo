# Auto-Land Pipeline & Diff Lifecycle

- `auto_land_blocked` with reason ≠ `panel_infra` is terminal — no replay on restart, no re-review; human Accept is the only unlock (main-epoch-26) (main-epoch-25) (main-epoch-22)
- Repair chain is permanently closed above the hard 64 KB repair-prompt cap (settle.go:573, "no silent truncation"); Manual Accept is the only unlock at that size — precedent diffs #41/#42/#45/#46, with #46 at 102,296 bytes (main-epoch-33) (UI-epoch-5) (UI-epoch-4)
- Manual accept decisions are justified by faithful reproduction: diff #40 was accepted after a byte-identical 499s full-suite reproduction proved `TestPlaywrightBrowsersDir` was a pre-existing deterministic env defect, not the diff (main-epoch-26)
- Panel verdicts can be factually refuted then manually landed: diff #41's glm rejection collapsed on a 9-callsite capDetail audit; landed as cf09b9e with a ledger-correction journal line; store entry deliberately left `rejected` (correction, not silent DB mutation) (main-epoch-27)
- Load flakes must not burn a pipeline rerun: diff #42 was accepted despite `verify_failed` because identical patch bytes passed verify minutes earlier and the failing spec passed 6/6 solo — rerun had zero information gain and re-exposed the #41 false-reject risk (main-epoch-29)
- Patch regeneration via drain semantics (`git add -A && git diff --cached HEAD`) before panel is safe only when no `patch_sha16` attestation is bound; after panel review, landed bytes must match attested bytes (main-epoch-22) (main-epoch-31) (main-epoch-33)
- Restart pending-diffs recovery dedups via a terminal-row classifier (`recoverPendingDiffs`): `auto_land_blocked`≠panel_infra, `moa_review{actor:auto_panel}`, and `auto_revise_round` skip re-fire; breadcrumbs/`panel_infra`/human rows still re-fire; journal read failure fails closed, abandoning the whole pass (UI-epoch-9)
- `.odo/diffs/` is an append-only archive, not a live queue — accepted patches are retained; wrong ledger entries get journal correction lines, never deletion (main-epoch-22) (main-epoch-27) (main-epoch-20)
- `capDetail` keeps the tail (rune-safe 4KB cut, `…[earlier truncated]` prefix) so `--- FAIL` lines survive in `verify_failed` journal details — head-trimming previously forced blind reproduction for every block (main-epoch-26) (main-epoch-25)
- All accept paths (autoland ×2, settle, loop_run, manual IPC) funnel through `handleDiffAction`; no staged-gate bypass exists (main-epoch-33)
- Orphaned detached-HEAD commits must be cherry-picked to main immediately; epoch-18's two "landed" commits (incl. the schema-v3 goal anchor) were orphaned on a deleted worktree, requiring a second journal correction declaring the first correction's premise invalid (main-epoch-20)
