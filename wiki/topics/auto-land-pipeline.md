# Auto-Land Pipeline & M20 Autonomy

- M20 removed the human-review gate: diffs are armed by default, `off` remains a kill-switch, no review models ⇒ unarmed silent skip; panel unanimous/mixed reject now auto-rejects with actor=auto_panel + evidence row, and base_stale conflict auto-revises at current HEAD via the ladder with chain supersede cleaning the old diff (main-epoch-8)
- Policy blocks (protected_path / supply_chain / verify / infra) stay pending as anomalies, not review; the 9 protectedGateFiles in internal/ipc (autoland, autonomy, learner, review, settle, ledger, risk, contradiction, design_moa) can never auto-land — a diff weakening the gate is blocked and accepted only by human click, pinned by TestHumanAcceptGateSourceAllowed (main-epoch-8) (bug-fix-epoch-1)
- M20 flipped the `auto_apply` default off→main (fail-closed, justified in settings.go); resulting test failures (TestGetSettings expected `off`; TestPhantomDiffVerdictBlocksAutoLand prefs lacked a `review:` line so the arming gate never armed) were diagnosed as stale tests, not production regressions (main-epoch-9)
- Pipeline stage visibility: daemon journals auto_land_started{stage:verify|panel} before runVerifyGate and before reviewFanout with no risk receipt (liveness is not an outcome); free gates return in ms with zero started rows, so a bare 'queued' >a few seconds is anomalous; true stuck = no journal rows + >~25 min silent window (verify 10min + 900s/leg panel) (bug-fix-epoch-1)
- Manual external lands are invisible to the pipeline (it only recognizes its own git apply) — diff-20 became a pending zombie pinning its worktree; settled via one BEGIN IMMEDIATE SQL accept + review_action note; root fix is a reverse-apply probe in checkAndRefreshBase (git apply --reverse --check ⇔ post-image already in main → settle already_landed) (main-epoch-8)
- Restart pending-diffs dedup (diff #25): recover-pending-diffs skips already-adjudicated diffs, self-evidenced by log 'recover-pending-diffs: 1/1 pending diffs already adjudicated — skipping their re-fire' (UI-epoch-10)
- Daemon-side reject via .odo/odo.sock (reject_diff) has no pipeline lock — only acceptMu + status checks — so human reject always passes even when the GUI buttons are locked; used to reject #21 directly (UI-epoch-10)
- Stale-test fixes are folded into an existing diff's snapshot rather than shipped as a separate hotfix; after rebase the snapshot's base_sha is set to the post-accept HEAD; mechanics: worktree.Manager.ExtractDiff writes worktree-vs-HEAD to .odo/diffs/<runID>.diff and UpdateDiffBaseSHA is the post-rebase refresh path (main-epoch-10)
- GUI human-lock bug (#26): auto_revise_product journal rows are pure bookkeeping (drainRun Fix B1); derivePipelineStates.latestChainRow treated the latest auto_panel row as state → in_flight → pipelineHumanLocked=true permanently since the row is never superseded on the origin; fix skips such rows so the origin falls back to the round row → revise phase → human-decidable ('accept origin to terminate ladder early' is intentional design; daemon semantics loop_run.go: 'revise done, product self-manages') (UI-epoch-10)
- Landing-order discipline for overlapping diffs: heaviest-evidence diff lands first, the other rebases onto new HEAD and re-verifies (order #24 → #22 because both heavily modify internal/ipc/server.go) (main-epoch-9) (main-epoch-10)
