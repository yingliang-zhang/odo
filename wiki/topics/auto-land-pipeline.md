# Auto-Land Pipeline & Diff Governance

- Auto-land flow: drain snapshots the worktree diff, verify gate runs, panel judges, then land; `auto_land_blocked` rows with any reason except `panel_infra` are terminal — diffs never auto-retry, manual Accept is the only unlock (main-epoch-40) (main-epoch-22) (main-epoch-26)
- `panel_infra` is the only non-terminal blocked reason; its designed resolution is daemon-restart re-fire via `recoverPendingDiffs` (main-epoch-17)
- Restart recovery dedup (Plan A): `pipelineTerminalDiffIDs` classifies terminal verdict rows (blocked≠panel_infra, `moa_review{actor:auto_panel}`, `auto_revise_round`) and skips those diffs; only genuinely stranded shapes (crumbs, panel_infra, human-only rows) re-fire; journal read failure aborts the whole pass fail-closed (UI-epoch-9)
- Superseded diffs should be rejected before accepting their successor so a daemon restart cannot re-fire them (51 rejected before 52 accepted; rescue snapshots and the permanent archive make this safe) (main-epoch-41)
- Manual-accept doctrine: after byte-level evidence chains (archive sha16 match, payload attribution, same-bytes reproduction) prove a block is a flake or stale verdict, human Accept overrides; precedents #41, #42, #45, #46, #48, #49, #52 (main-epoch-41) (main-epoch-37)
- M20 cut: pipeline armed by default (`off` kill-switch retained), panel-consensus reject auto-rejects, base-stale enters the auto-revise ladder, GUI accept/reject remains as triage valve not required path; unarmed iff zero review models (main-epoch-8) (main-epoch-9)
- Zombie settlement: `checkAndRefreshBase` reverse-apply probe (`git apply --reverse --check`) settles externally-landed diffs as `already_landed`; diff-20 was settled out-of-band via one BEGIN IMMEDIATE transaction (status update + same-shaped `review_action` journal row, empty actor = human) (main-epoch-8)
- Reject deletes the candidate worktree, once leaving the `.diff` as sole surviving artifact (recovery incident #47); hardened with `rescueResolvedWorktree` snapshot writing `<run>-rescue.diff` pre-retirement when the worktree delta differs from the archived patch (main-epoch-35) (main-epoch-36)
- Verify diagnostics: full gate output persisted to `.odo/verify/<label>-<ts>.log` (1MB tail cap, prune newest 32) because 4KB `capDetail` truncation repeatedly ate `--- FAIL:` lines; `capDetail` flipped head-trim → tail-bias with rune-safe cut so FAIL lines survive in blocked journal details (main-epoch-36) (main-epoch-25) (main-epoch-26)
- Revise ladder wedge: `startReviseRun` repair agents staging into sibling worktrees produced forever-silent pending origins (incidents #81/#83); fixed by loud `revise_product_missing` journal at drain (no false-stop retry) plus canonical-worktree declaration embedded in both ladder prompts, with worktree created before prompt assembly; `startFollowupRunLocked` gained `originDiffID` propagation (bug-fix-epoch-13)
- Worktree discipline: the run's own bound worktree is the only canonical checkout — nothing staged elsewhere exists; `drainRun` extracts only `meta.worktreePath` (bug-fix-epoch-12) (bug-fix-epoch-13)
- Repair-chain structural limit: diff bodies >64KB cannot enter the auto-repair chain (`settle.go` cap, "no silent truncation ever"); manual Accept is the only unlock at that size (#46 at 102KB) (main-epoch-33)
- Revise round cap is 2 (`settleMaxReviseRounds 3→2`): rounds 1–2 require unanimous accept else auto-reject; terminal round-3 evaluation uses the majority valve (main-epoch-21)
- Memory-path diffs are refused at registration in `drainRun` (same SSOT predicate as the accept layer) with transcript advisory naming paths and salvage patch kept in `.odo/diffs/`; double-layer structure (pre-panel block + accept backstop) retained (main-epoch-24)
