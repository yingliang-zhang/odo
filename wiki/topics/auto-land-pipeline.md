# Auto-Land Pipeline (M16)

- consensusVerdict 2/3 tally failed open (2 ACCEPT + 1 NEEDS_FIXES counted as accept); M16 flipped it to unanimity — a lone needs_fixes or reviewer error now blocks, GUI label says "unanimous gate" (epoch-8)
- Design panel verdict DROP_SIZE_KEEP_DIR (3/3): line-count gates dropped as fake precision (300 lines ≈ 1–3% of 350K contexts; 3/8 dogfood diffs exceeded it); new-top-dir gate kept; supply-chain basenames (manifests/lockfiles/.odo-verify) and weakened-tests (net assertion loss) gates added at near-zero cost; an 87K-token prompt cost breaker is the only ceiling — genuine input overflow degrades to needs_fixes via API error anyway (epoch-8)
- Gate order: pref auto_apply==main (fail-closed parse, pref-off silent) → clean run → mechanical autoLandCheck → mandatory .odo-verify at the run's worktree root (10-min cap, 8KB tail; absent/failed always blocks) → cost breaker → grounded adversarial panel (journaled user trigger text, never the agent's self-report; verdict last) → unanimity → moa_review journaled BEFORE handleDiffAction(actor=auto_panel) (epoch-8)
- ComputeAutonomy tallies auto_panel accepts into AutoAccepted, never into rung streaks — the ratchet must not drink its own bathwater (epoch-8)
- Arming is deliberately manual and three-layer: prefs auto_apply: main, daemon rebuild + restart (shipped code ≠ running code), one human accept of the .odo-verify diff itself (supply-chain self-modification can never auto-land) (epoch-8)
- Terminal panel harness for daemon-free tri-model review: ODO_PANEL_LIVE=1 ODO_PANEL_PROMPT=<file> ODO_PANEL_OUT=<file> go test ./internal/ipc/ -run TestPanelLive (default-skipped, never in the suite) (epoch-8)
- Panel harness spin trap: offered tools + 16-round cap ⇒ models page server.go in 400-line windows and never answer (3/3 loop-exceeded at first M16 review attempt). Working recipe: inline the gate stack verbatim with line numbers into the prompt + an explicit 7-read batched READ PLAN (offsets given) + tool-economics warning; 3/3 then completed in ~4 min with 12–15 calls (epoch-8)
- M16 fix-round panel (HEAD 50e3e32): 3/3 NEEDS_FIXES, architecture endorsed. Landed fixes: (1) parseVerdict FINAL verdict-line wins (was first-match → early-ACCEPT/stray-token bypass) + truncated review forced needs_fixes via reviewVerdict — shared by manual review path; (2) runVerify allowlisted child env verifyEnviron (verify executes unreviewed agent code; daemon keys SUDO_*/AWS_* stay out — GOPROXY basic-auth rides GO-prefix, documented residual); (3) base_stale gate — verify attested base+diff but land applies onto current HEAD, so HEAD must equal base_sha before any spend; (4) TestAssertionDelta comment-aware (+// assert.X no longer nets zero); (5) autoLandPrompt fences the diff body as data (epoch-8)
- Deferred post-panel: process-group sweep on verify timeout, min panel-size policy (N=1 unanimity is trivial), merge-preview verify as throughput upgrade over base_stale, repeated-block GUI surfacing, stubbed-reviewer land-path integration test (epoch-8)
