# Steer Queue

- The daemon already was a queue (handleSteering appends to meta.queuedSteers, drained at run end as continuation runs); the work added the GUI visibility layer + fail-closed daemon fixes: orphan-steer rejection, journal seqs on queuedSteers + steer_seqs on run_prompt receipts, drop_queued_steer IPC, and batched steer_dropped{cause} journaling so every journaled steer ends consumed or dropped (UI-epoch-4) (UI-epoch-6)
- Deliberate Hermes deviations: queue items not editable (Drop + retype — no un-steer edit channel in the daemon), journal-backed instead of localStorage (cross-restart/multi-window consistency) (UI-epoch-4)
- Diff #9 was REJECTED by the panel with three real defects — ghost queued steers made undeletable by daemon restart, drop two-stage confirm reset by poll heartbeat, mixed go+gui diff running only go verify — auto-fix exhausted the 85KB > 64KB cap; fix round landed as #11 (d709366): recoverOpenSteers journal recovery on startup, drop-confirm state preserved across poll re-derivation, verify running all applicable .odo-verify matches (UI-epoch-5) (UI-epoch-6)
- Production evidence for #11: the binary-swap restart closed 3 historically dangling steers via recoverOpenSteers (steer_dropped{cause:daemon_restart}) — no new drops since; drop-confirm poll-resistance remains verified only at e2e level and still lacks a manual GUI test (UI-epoch-6)
- Steer-queue GUI consumes journal-only derivation in steer_queue.ts (user_message{steer:true} minus run_prompt.steer_seqs and steer_dropped closures) — survives restarts/session switches with no new polling (UI-epoch-4)
