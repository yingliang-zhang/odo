# Daemon Deployment, Crashes, and Recovery Hardening

- Deploy rule: atomically replace the binary (cp-to-temp + mv) at BOTH <project>/odo and ~/.odo/bin/odo BEFORE killing the daemon — otherwise the GUI's ensure_daemon_running auto-respawns the old binary (UI-epoch-6) (main-epoch-27) (UI-epoch-5)
- Liveness proof requires more than on-disk sha: go version -m vcs.revision + PID/socket health + behavioral checks (schema_version=3, diffs.goal column in journal.sqlite) — epoch-20's built-but-never-swapped binary proves disk state is insufficient (main-epoch-21)
- #41 binaries redeployed as cf09b9e (both locations, permissions preserved); #42's dirty-path/retireRun guard logic takes effect only after a redeploy + restart because the running daemon (PID 768) is on the old image (main-epoch-27) (main-epoch-29)
- hermes-agent daemon (PID 85939, up since 08-20) still runs an old binary from the shared ~/.odo/bin/odo; restarting would kill its sessions — user decision pending since epoch-23, no divergence urgency (per-project stores) (main-epoch-27) (main-epoch-29)
- Daemon handover needs no nohup: ensure_daemon_running (lib.rs) auto-relaunches the daemon on next command failure with flock single-instance; a bare `odo help`/`odo` probe can spawn an unintended daemon (incident pattern, self-cleaned); an old binary run inside a worktree path hangs during daemon bootstrap after 'refusing to register worktree path' — read-only journal commands work, CLI defect unfixed (UI-epoch-5) (UI-epoch-6) (main-epoch-27) (main-epoch-29)
- Odo.app deploy pipeline: rebase + FF merge + npm run tauri:build in main checkout (~30s warm) + ditto replace /Applications/Odo.app + relaunch; verification = SHA-256 installed binary vs bundle artifact + running PID txt mapping + main HEAD check (UI-epoch-3) (UI-epoch-7)
- modernc.org/sqlite SIGBUS (WAL _walIndexRecover mmap crash) has recurred (2× on 08-11, 15:36 on 08-20): unresolved; first remedy _pragma mmap_size=0 in the connection string, upgrade dependency only if that fails (UI-epoch-10) (UI-epoch-11) (bug-fix-epoch-4)
- SIGQUIT immunity (installSIGQUITImmunity): signal.Notify + log + discard — graceful srv.Wait on QUIT would hang shutdown up to ~24 min on in-flight panels; SIGABRT retained for deliberate dumps; smoke-verified kill -QUIT survives with socket serving (bug-fix-epoch-4)
- Boot recovery order matters: orphan-request sweep runs FIRST in NewServer — non-control user_messages without agent_done/agent_error get agent_error{cause:daemon_restart} (GUI renders failure bubble instead of hanging); steer/park//loop control rows are skipped; self-excluding on next fold → idempotent (bug-fix-epoch-4)
- 'Daemon restarted while this request was in flight' bubbles are orphan markers stamped on in-flight requests at daemon death; they persist in history and do not indicate current failure (UI-epoch-10)
- Panel progress heartbeat is memory-only (panelProg map, never journaled; poll handshake copies the map to avoid an encode race): GUI spinner shows '· N/M back', visible even across concurrent panels and GUI reopen mid-advisory (bug-fix-epoch-4)
- Dev-dir disk cleanup: 6.6 GB was 98% Rust build output (src-tauri/target 6.3 GB); deleting target/ + gui/node_modules/ shrank the project to 128 MB; tracked source archived via git archive HEAD zip (469 files, cf09b9e) (main-epoch-27)
- An unexplained 22:07 external SIGQUIT killed the daemon mid-panel (no code path in repo sends SIGQUIT; sender unattributable); a 22:48 'unresponsive' /panel was just slow (legs journaling, ~6 min prior precedent) — distinguishable via daemon.log + WAL write activity (bug-fix-epoch-4)
