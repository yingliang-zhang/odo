# Review Panel & Grounded Legs

- Grounded tool-round cap raised 16->40 with defaultToolRounds staying 16 so zero-value callers keep prior semantics (clamp pins 0->16, 16->16, 40->40, 50->40); the server cap resolves field > env ODO_GROUNDED_TOOL_ROUNDS > prefs grounded_tool_rounds > 40, and rollback is config-only with zero verdict/gate drift (bug-fix-epoch-39)
- Wall-clock interlock: rounds > 16 scales the grounded leg's outer deadline by rounds/16 (review base s.legTimeout, audit base moa.TimeoutForModel, base asymmetry preserved) so the typed infra death stays round capacity rather than a misleading timeout; worst-case leg wall clock rises ~31min -> ~78min, which is the lock's intent (bug-fix-epoch-39)
- ToolRoundsUsed is journaled on every grounded leg row from pre-truncation len(calls), with round-cap death rows carrying call names/args and a fail-hard marker kept distinct from fail-soft tool_budget_exhausted; groundedToolCallsCap rose 64->96 so the audit trail survives 40 rounds (bug-fix-epoch-39)
- The byte-budget graceful test is lock-literal: 39 tool rounds execute and the 40th post delivers the verdict, read_bytes > 256KB is asserted so the budget provably tripped, and the assertions distinguish budget exhaustion from any round-cap death (bug-fix-epoch-39)
- Tool-loop exhaustion marks a leg Infra=true and parks the round blocked-pending (recoverable) rather than counting a synthesized needs_fixes as real dissent — a burned-out tool loop is not direction evidence (bug-fix-epoch-27)
- parseVerdict normalizes markdown-decorated verdict lines to a fixpoint (uppercase -> strip leading #*_> decoration -> strip one optional VERDICT: label -> strip leading decoration again -> strip trailing decoration) so all three panel models' forms parse; the cutset contains only decoration characters, so ACCEPTANCE CRITERIA: can never collapse to a bare token and fail-closed semantics are unchanged by construction (bug-fix-epoch-43)
- Before that fix the Tier-1 memory gate was structurally dead: of 300 proposal-review legs, 223 wrote text-verdict ACCEPT but only 39 parsed (61% of accept-grade legs misfiled as needs_fixes), with 4 memory_apply events ever and 0 accepted — the fix targets the parser, not the models (bug-fix-epoch-43)
- Settle revise prompts over 128KB carry a needs-based digest: per-file stat block plus full hunks of only the files non-accept feedback names, with an elision trailer pointing at the on-disk patch; the named set is patch-paths intersected with feedback path-tokens so absent files are never included, and the 256KB hard cap stays as last resort on the digested input (bug-fix-epoch-29)
- The repair chain is permanently closed above the 64KB diff-body cap (no silent truncation, ever) with revise rounds capped at 2 — manual Accept is the only unlock at that size (main-epoch-33)
- Gate-source diffs require unanimous panel attestation (the majority valve has no attestation power over them), and every accept path (autoland, settle, loop_run, manual IPC) funnels through handleDiffAction — no staged-gate bypass exists (main-epoch-33)
- A NEEDS_FIXES aggregation can be infra-degraded: when both visible reviewers accept and legs die on the very round-cap the diff fixes, the round carries no directional rejection — assess before reworking (bug-fix-epoch-39)
