{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs plus app commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
